<UserControl
    x:Class="kanban.BoardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:kanban"
    x:Name="boardView">
    <Grid Padding="16,0">
        <ScrollViewer
            x:Name="BoardScrollViewer"
            HorizontalScrollBarVisibility="Auto"
            HorizontalScrollMode="Enabled"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Enabled"
            ZoomMode="Disabled">
            <Grid x:Name="BoardWheelSurface" Background="Transparent">
                <ListView
                    x:Name="ColumnRepeater"
                    Padding="0,0,320,240"
                    VerticalAlignment="Stretch"
                    AllowDrop="True"
                    Background="Transparent"
                    BorderThickness="0"
                    CanDragItems="True"
                    CanReorderItems="True"
                    DragItemsCompleted="OnColumnDragItemsCompleted"
                    DragItemsStarting="OnColumnDragItemsStarting"
                    IsItemClickEnabled="False"
                    ItemsSource="{x:Bind ViewModel.Columns, Mode=OneWay}"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.HorizontalScrollMode="Disabled"
                    ScrollViewer.VerticalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollMode="Disabled"
                    SelectionMode="None">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel VerticalAlignment="Top" Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="VerticalAlignment" Value="Top" />
                            <Setter Property="VerticalContentAlignment" Value="Top" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:ColumnData">
                            <local:BoardColumn VerticalAlignment="Top" ColumnData="{x:Bind}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
